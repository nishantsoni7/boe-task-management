'use client'

import { BatchesScreen } from '../BatchesScreen'

// Generate → review → approve → assign, in the order the work happens.
export default function BatchesPage() {
  return <BatchesScreen />
}
