// Public layout for customer-facing showroom pages.
// No sidebar, no auth, no BOE OS chrome.
export default function ShowroomLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
